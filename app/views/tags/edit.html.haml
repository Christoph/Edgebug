%h1 Editing tag

- form_for(@tag) do |f|
  = f.error_messages

  %p
    = f.label :name
    %br
    = f.text_field :name

  %p= f.submit "Update"

= link_to 'Show', @tag
|
= link_to 'Back', tags_path

%h1 Listing tags

%p this should later be ordered by usage (most used on top)
- i = 0
- for tag in @tags
  .result
    %span.result_number
      = i += 1
    %span.result_tag
      = link_to tag.name, tag
    Used
    = tag.testcase_ids.length
    times.
    %span.result_latest_usage
      - last_case = tag.testcases.last
      - if last_case
        Recent usages:
        = link_to last_case.title, last_case unless last_case == nil